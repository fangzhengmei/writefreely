# 文章编辑与发布流程

## 一、两条内容路径

系统中有两类内容，共用同一份存储，但行为模式不同：

- **草稿**：不归属于任何博客的独立文章，只有作者本人在"我的草稿"列表中能看到
- **集合文章**：发布到某个博客下的文章，博客的读者可以看到

判断一篇文章属于哪一类，看它是否关联到了某个博客。

---

## 二、列表页面的权限隔离

列表页是用户看到内容的第一道筛选，不同类型的列表有不同的筛选逻辑。

### 草稿列表
你在"我的草稿"页面看到的内容，在数据查询阶段就已经限定为只返回你自己的文章。不需要额外的权限检查，因为查询条件本身就决定了你只能看到自己的。

也没有时间过滤——你当然可以看到自己设置了未来发布时间的草稿。

### 博客文章列表
访客访问某个博客的首页时，筛选逻辑分两步：

第一步，先检查这个博客本身能不能被访问：
- 如果博客是私有的，非作者会看到"页面不存在"（不暴露这个博客的存在）
- 如果博客是密码保护的，非作者需要输入密码才能继续
- 如果博客是公开的，直接放行

第二步，查询这个博客下的文章：
- 只返回属于这个博客的文章
- 非作者访问时，自动过滤掉发布时间在未来的文章
- 作者本人可以看到所有文章，包括还没到发布时间的（会标记"定时发布"）

---

## 三、编辑页面的权限限制

点"编辑"进入编辑器时，两类文章的检查方式不一样。

### 草稿的编辑
进入草稿编辑器时，只做两件事：
1. 确认你已登录
2. 根据文章 ID 把内容取出来

不会额外验证你是不是这篇文章的作者。原因是：
- 你只能从"我的草稿"列表进入编辑页，那个列表本身就只显示你的文章
- 草稿的地址是随机字符串，别人很难猜到

但这也意味着，如果有人碰巧知道了某篇草稿的完整编辑地址，理论上可以打开编辑器页面。

### 集合文章的编辑
进入博客文章编辑器时，检查更严格：

1. 确认你已登录
2. 根据文章别名和博客名找到这篇文章
3. **必须验证你是文章的作者**，否则跳转到文章公开页

为什么多了作者验证这一步？
- 博客的文章地址是公开的，任何人都能看到文章 URL 和编辑按钮的位置
- 如果不验证作者身份，任何登录用户都可以尝试编辑别人的文章

### 重要：博客的可见性不限制编辑页
博客的私有/密码保护设置，主要影响的是**公开浏览**（博客首页、文章详情页等访客会看的页面）。

编辑页面走另一条路径，不会先检查博客的可见性设置。对于作者本人来说，无论博客设为私有、公开还是密码保护，都不影响自己进入编辑器。

换句话说：博客可见性控制的是"别人能不能看到你的文章"，不是"你能不能编辑自己的文章"。

---

## 四、实际保存修改时的权限

无论是草稿还是集合文章，真正保存修改时，都会在数据层确认一次：只有文章的所有者才能修改它。

这是最终的安全防线。即使前端的检查有遗漏，保存这一关也会拦住非作者的修改。

---

## 五、定时发布的可见性

定时发布的意思是：把文章的发布时间设为未来的某个时刻。

### 列表中的表现
- 作者能看到这篇文章，标题旁有"定时发布"标记
- 访客看不到这篇文章（列表查询时被过滤掉了）

### 详情页的表现
如果有人知道文章的完整地址：
- 作者和访客都可以直接打开看到全文
- 不会因为发布时间还没到就阻止访问

### 为什么列表和详情不一致
这是设计取舍，不是 bug。逻辑是这样的：
- 文章的地址通常由标题自动生成，外人很难猜到
- 列表页是主要的发现入口，列表中不显示就等于"没发布"
- 如果你作为作者主动把地址分享给别人预览，那是你授权的

换句话说，定时发布的"保密"是靠"不在列表中出现"实现的，不是靠详情页的权限检查。

### 草稿列表的特殊情况
在"我的草稿"列表中，即使某篇草稿设置了未来发布时间，也不会显示"定时发布"标记。因为草稿本身就等于"未发布"，这个标记只有对已经发布到博客的文章才有意义——表示"已发布但还没到公开时间"。

---

## 六、已删除内容的处理

当一篇文章的标题和正文都被清空时，它就进入了"已取消发布"状态。

此时访问它的地址，不同路径返回的结果略有不同：
- 浏览器访问：显示"已删除"页面
- 以数据格式访问独立文章：返回成功状态码，但内容是错误信息
- 以数据格式访问博客文章：返回"已删除"状态

博客文章的处理更一致，一律按已删除处理。独立文章对数据格式做了兼容处理，是历史原因。

---

## 七、设计思路总览

每个设计决策都是为特定场景优化的：

| 设计 | 适用场景 | 背后逻辑 |
|------|---------|---------|
| 列表查询时就限定只返回本人的 | 草稿 | 草稿是私密的，查询层过滤最简单可靠 |
| 编辑器不重复验证作者身份 | 草稿 | 列表已过滤，地址难猜 |
| 先检查博客可见性再查文章 | 博客列表页 | 博客有公开/私有/密码保护之分 |
| 编辑器必须验证作者身份 | 集合文章 | 博客地址是公开的，任何人都可能尝试编辑 |
| 博客可见性不限制编辑页 | 集合文章 | 可见性管的是读者，不是作者自己 |
| 列表过滤未来文章，详情不过滤 | 定时发布 | 靠不公开实现保密，不防主动分享链接 |
| 保存时在数据层验证所有者 | 所有修改 | 最终安全防线 |
